server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3311/co_ownership_booking_admin?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
    
    admin:
      url: jdbc:mysql://localhost:3311/co_ownership_booking_admin?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      username: root
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
    
    booking:
      url: jdbc:mysql://localhost:3310/co_ownership_booking?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      username: root
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  devtools:
    restart:
      enabled: false

jwt:
  secret: SuperSecretKey123

sync:
  schedule:
    cron: "0 */5 * * * ?"


