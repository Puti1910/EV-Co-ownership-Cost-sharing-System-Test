# Docker-specific configuration for UI Service
# Override cost-payment.service.url to call directly to cost-payment-service (avoid routing loop)

# Cost Payment Service - call directly to service (not via API Gateway for internal calls)
cost-payment.service.url=http://cost-payment-service:8081

# Keep other services via API Gateway
group-management.service.url=${API_GATEWAY_URL:http://api-gateway:8084}
user-account.service.url=${API_GATEWAY_URL:http://api-gateway:8084}
reservation.service.url=${API_GATEWAY_URL:http://api-gateway:8084}
reservation.admin.service.url=${API_GATEWAY_URL:http://api-gateway:8084}

